package Core;

public class GarbageCollector {
    /*Сборщик мусора выполняет 2 задачи
        Поиск мусора
        очистка мусора

      Для обнаружения мусора есть 2 подхода
      REFERENCE COUNT(Подсчет ссылок)
      Каждый объект имеет счетчик, он хранит количество ссылок указывающих на объект
      Если значение счетчика = 0, значит этот объект мусор и он удаляется

      TRACING
      Живые объекты те до которых мы можем добраться от КОРНЯ(GC Root),
      в то время как остальные - мусор.

      Есть 4 типа корневых точек
      -Локальные переменные и параметры методов
      -Потоки
      -Статические переменные
      -Ссылки из JNI
      (JNI (Java Native Interface) - это механизм, который позволяет коду на
      Java взаимодействовать с нативным кодом (C/C++) и наоборот.
      Когда нативный код работает с объектами Java, он использует специальные ссылки.)


    Основные поколения в HotSpot JVM

    1. Young Generation (Молодое поколение)

    Здесь создаются все новые объекты. Делится на три части:
    Young Generation
    ├── Eden (Эдем) - все новые объекты
    └── Survivor Spaces (Выжившие)
        ├── S0 (From)
        └── S1 (To)

    Процесс в Young Generation:

    1. Объекты создаются в Eden
    2. Minor GC - когда Eden заполняется:
       · Живые объекты из Eden копируются в S0
       · Все остальные объекты в Eden удаляются
    3. При следующем GC:
       · Живые объекты из Eden и S0 копируются в S1
       · S0 становится пустым, роли меняются
    4. Объекты, пережившие несколько циклов (обычно 15), перемещаются в Old Generation

    2. Old Generation (Старое поколение)

    Здесь хранятся долгоживущие объекты.

    · Major GC / Full GC - сборка Old Generation
    · Происходит реже, но дольше
    · Обычно использует алгоритмы "mark-sweep-compact" или "mark-copy"

    3. Metaspace (до Java 7 - PermGen)

    MetaSpace - новая область памяти появившаяся в Java 8 и заменившая устаревшую
    Perm Generation. Основное их отличие заключается в способе распределения памяти.
    По умолчанию MetaSpace увеличивается автоматически.

    Хранит метаданные классов, статические переменные, пулы констант.

    · Не является частью кучи
    · Растет автоматически (можно ограничить)
    · Собирается при выгрузке классов
     */

}
