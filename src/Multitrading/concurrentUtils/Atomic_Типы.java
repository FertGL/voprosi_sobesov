package Multitrading.concurrentUtils;

public class Atomic_Типы {
    /*
    Atomic в Java (пакет java.util.concurrent.atomic) — это набор классов,
    которые обеспечивают потокобезопасное выполнение операций над одной переменной
    без использования синхронизации (synchronized).

    Они используют низкоуровневые механизмы, такие как Compare-and-Swap (CAS),
    что делает их быстрее и эффективнее традиционных блокировок.

    Зачем они нужны?

    Рассмотрим классическую проблему: инкремент (count++).
    Даже такая простая операция на самом деле состоит из трех шагов (чтение -> изменение -> запись).
    Без синхронизации два потока могут прочитать одно и то же значение,
    увеличить его и записать обратно, в результате чего одно изменение потеряется.

    Как они работают (CAS)?

    Секрет производительности атомиков — использование оптимистичной блокировки.

    1. Поток читает значение из памяти.
    2. Пытается вычислить новое значение.
    3. Выполняет одну машинную команду CAS (Compare-and-Swap):
    "Если значение в памяти до сих пор равно тому, что я прочитал (его никто не изменил),
    то меняю его на новое".
    4. Если значение изменилось (другой поток успел вмешаться), операция начинается заново
    (в цикле).

    Основные классы

    Для работы с разными типами данных:

    · AtomicInteger, AtomicLong — для чисел.
      · incrementAndGet(): ++i
      · addAndGet(delta): i = i + delta
      · get(): получить значение.
    · AtomicBoolean — для флагов.
    · AtomicReference — для ссылок на объекты (позволяет менять ссылку атомарно).
    · LongAdder — специальная версия для высоконагруженной статистики (счетчиков).
        Она работает быстрее AtomicLong, когда много потоков пишут, но читают редко
        (жертвует памятью ради скорости).

    Когда использовать?

    1. Счетчики (хиты, статистика).
    2. Генерация последовательностей (ID, номера заказов).
    3. Флаги состояния (например, initialized).
    4. Неблокирующие алгоритмы (Non-blocking algorithms).

    Преимущества

    · Высокая производительность — нет переключения контекста, как при synchronized.
    · Отсутствие Deadlock (взаимной блокировки потоков) — так как поток не блокируется,
        а крутится в цикле.
     */
}
